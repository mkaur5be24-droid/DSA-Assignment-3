#include <iostream>
#include <stack>
#include <string>
using namespace std;
int main(){
  string s;
  getline(cin,s);
  stack<char> st;
  for(char c:s) st.push(c);
  for(size_t i=0;i<s.size();++i){
    s[i]=st.top();
    st.pop();
  }
  cout<<s;
  return 0;
}
